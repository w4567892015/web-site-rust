<system>
  workers 2
</system>

<worker 0-1>
  <source>
    @type forward
    bind 0.0.0.0
    port 24224
    <parse>
      @type bunyan
    </parse>
  </source>
</worker>

<match **>
  @type stdout
</match>
